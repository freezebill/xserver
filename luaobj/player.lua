local obj = require "obj"

local player = obj.new( "player" )

-- base ----------------------------------------
function player.onNew( )

    -- player.startUpdate( 100, 1000 ) / player.onUpdate100() player.onUpdate1000() / player.stopUpdate( )
end

function player.onClear( )

end

-- base ----------------------------------------



return player